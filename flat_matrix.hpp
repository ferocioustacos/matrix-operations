#include "matrix.hpp"

class FlatMatrix {
    typedef Matrix::data_t data_t;
    size_t n, m;
    size_t size;
    std::vector<data_t> M;

    
    // TODO: redefine matrix functions but with flattened indexing
};